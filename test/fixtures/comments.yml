# Read about fixtures at
# http://api.rubyonrails.org/classes/ActiveRecord/Fixtures.html

one:
  user: users(:user)
  memory: memories(:one)
  body: This is in response to the first memory

two:
  user: users(:user)
  memory: memories(:two)
  body: Monkey Monkey
